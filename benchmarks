#!/usr/bin/env bash

# DATE_STR=$(date +"%Y-%m-%d")
# SCALA_URI_VER=$(cat build.sbt | grep -E "com\.github\.theon.+scala-uri" | cut -d'"' -f6)

sbt "test-only *.ParsingBenchmark -- -CresultDir parsing"
sbt "test-only *.RenderingBenchmark -- -CresultDir rendering"